import styled from 'styled-components'

const SectionTitleStyled = styled.div`
  display: inline-block;
  font-size: 2.4rem;
  margin-bottom: 4rem;

  img {
    position: absolute;
    bottom: -.8rem;
    right: -2.4rem;
  }

  span {
    display: inline-block;
    margin-left: 1.6rem;
    font-weight: 400;
  }
`

export {
  SectionTitleStyled
}
